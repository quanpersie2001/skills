{
  "kind": "pulse-plugin-benchmark",
  "schemaVersion": 2,
  "version": 2,
  "mode": "chat",
  "scenarioSource": "pulse-eval-workspace/evals.json",
  "pilotScenarioIds": [21, 2, 7, 8, 15],
  "shared": {
    "verifiers": [
      "node .codex/pulse_status.mjs --json",
      "bash scripts/check-markdown-links.sh"
    ]
  },
  "notes": [
    "This is the focused Pulse pilot suite, not full-chain coverage.",
    "Run selected scenarios in the current interactive chat instead of spawning headless Claude or Codex sessions.",
    "Use the generated benchmark-packet.md and benchmark-evidence.json artifacts, then rerun the evaluator with --evidence to compile benchmark.json and benchmark.md."
  ]
}
